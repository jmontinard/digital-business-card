import React from 'react'

const Headshot = ({headshot}) => {
  return (
    <img className='headshot'  src={headshot} alt={`add prop for there name professional headshot`}/>
  )
}

export default Headshot